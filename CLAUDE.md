# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Repo Is

Personal knowledge graph for William Donald Martínez Avellan (Will Martinez) — software engineer, contractor + freelancer based in Nicaragua. Not a code project. Source of truth for career context, active projects, clients, and finances.

## Directory Structure

```
will_personal/        ← Obsidian vault (source of truth)
  Projects/           ← Active and legacy projects
  Companies/          ← Client + employer profiles
  Contacts/           ← People
  Finance/            ← Monthly finance snapshots
  Work/               ← Employment modes (Contractor, Freelance)
  Resume.md
  Will Martinez.md    ← Root identity node

graphify-out/         ← Generated knowledge graph (do not edit manually)
  GRAPH_REPORT.md     ← Graph summary, god nodes, communities
  obsidian/           ← Graph nodes as Obsidian-compatible .md files
  graph.json          ← Raw graph data
  graph.html          ← Visual graph explorer
```

## Key Entities

**Identity:** William Martinez (`will_personal/Will Martinez.md`)

**Active Work:**
- Contractor → In All Media → client: Pluto TV / Paramount
- Freelance → Enhancement Payment System, ALUVAL Nicaragua

**Primary Project:** Marco ERP (`will_personal/Projects/Marco ERP.md`)
- Multi-tenant ERP replacing legacy SEM (Visual FoxPro 6)
- Repos: `~/repos/marco-frontend`, `~/repos/marco-backend`
- Stack: React + NestJS + MongoDB + AWS Lambda/SQS/S3 + TypeScript
- Auth: JWT + `X-Tenant-ID` header for tenant isolation
- Key rules: integer cents (no floats), soft delete only, stock changes via `StockWarehouseMasterService.applyDelta`
- Potential clients: ALUVAL Nicaragua, Vidrieria FMorales

**Legacy:** SEM — Visual FoxPro 6 CRM, archived, still deployed at ALUVAL Nicaragua

## graphify-out

Generated by the `graphify` skill. Do not edit. Re-run `/graphify` to regenerate after vault changes.

- `GRAPH_REPORT.md` — start here for graph navigation, community hubs, god nodes
- God nodes (most connected): William Martinez → In All Media → Marco ERP → Enhancement Payment System
- Communities: Clients & Finance, Pluto TV Engagement, AWS & Payments, Early Career & Backend Stack, Banking & Infrastructure, Node/Vue/Frontend

## Working With This Repo

- Questions about Will's background, projects, clients, or stack → read `will_personal/` files directly
- Graph queries or "how does X connect to Y" → use `/graphify` skill or read `graphify-out/GRAPH_REPORT.md` first
- Adding new knowledge → edit files in `will_personal/`, then re-run `/graphify` to update the graph
- Finance data lives in `will_personal/Finance/` organized by year
